{
  "name": "dev-serv",
  "version": "0.1.1",
  "description": "开发时使用的专属服务器",
  "main": "src/dev-serv.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "dev-serv": "./bin/dev-serv"
  },
  "keywords": [
    "dev-server",
    "static-server",
    "mock-server",
    "proxy-server",
    "live-reload"
  ],
  "author": "Sun",
  "license": "MIT",
  "dependencies": {
    "express": "^4.14.1",
    "http-proxy-middleware": "^0.17.3",
    "puer": "^1.1.1",
    "puer-mock": "^1.2.0",
    "serve-index": "^1.8.0"
  }
}
